#!/usr/bin/env sh
set -euo pipefail
mkdir -p dist
tar --exclude="bin" --exclude="dist" --exclude="vendor" --exclude="tests" --exclude=".git" -zcf dist/artifact.tar.gz . > /dev/null
echo artifact.tar.gz
